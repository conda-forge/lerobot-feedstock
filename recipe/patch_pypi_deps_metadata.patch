diff --git a/pyproject.toml b/pyproject.toml
index b59d169..cae7409 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -66,17 +66,22 @@ dependencies = [
     "accelerate>=1.10.0,<2.0.0",
 
     # Core dependencies
-    "numpy>=2.0.0,<2.3.0", # NOTE: Explicitly listing numpy helps the resolver converge faster. Upper bound imposed by opencv-python-headless.
-    "setuptools>=71.0.0,<81.0.0",
-    "cmake>=3.29.0.1,<4.2.0",
+    # Upper bound is mainly to simplify PyPI solver behavior; conda-forge solver has global metadata.
+    "numpy>=2.0.0",
+    # setuptools is pinned upstream for wheel build workflows; not needed as a conda noarch runtime dependency.
+    # "setuptools>=71.0.0,<81.0.0",
+    # cmake is a wheel-build helper upstream and not required for conda-forge runtime metadata.
+    # "cmake>=3.29.0.1,<4.2.0",
     "packaging>=24.2,<26.0",
 
     "torch>=2.2.1,<2.11.0",
-    "torchcodec>=0.2.1,<0.11.0; sys_platform != 'win32' and (sys_platform != 'linux' or (platform_machine != 'aarch64' and platform_machine != 'arm64' and platform_machine != 'armv7l')) and (sys_platform != 'darwin' or platform_machine != 'x86_64')",
-    "torchvision>=0.21.0,<0.26.0",
+    # Upper bounds are ABI-related in PyPI workflows and are handled via conda-forge metadata constraints; platform selectors are not needed in conda-forge.
+    "torchcodec>=0.2.1",
+    "torchvision>=0.21.0",
 
     "einops>=0.8.0,<0.9.0",
-    "opencv-python-headless>=4.9.0,<4.13.0",
+    # Relax the upper bound in conda-forge; solver constraints are handled by conda package metadata.
+    "opencv-python-headless>=4.9.0",
     "av>=15.0.0,<16.0.0",
     "jsonlines>=4.0.0,<5.0.0",
     "pynput>=1.7.8,<1.9.0",
@@ -98,7 +103,8 @@ dependencies = [
 
 # Common
 pygame-dep = ["pygame>=2.5.1,<2.7.0"]
-placo-dep = ["placo>=0.9.6,<0.9.17"]
+# Placo upper bound is not required for conda-forge metadata.
+placo-dep = ["placo>=0.9.6"]
 transformers-dep = ["transformers>=5.3.0,<6.0.0"]
 grpcio-dep = ["grpcio==1.73.1", "protobuf>=6.31.1,<6.32.0"]
 can-dep = ["python-can>=4.2.0,<5.0.0"]
@@ -131,8 +137,10 @@ unitree_g1 = [
 reachy2 = ["reachy2_sdk>=1.0.15,<1.1.0"]
 kinematics = ["lerobot[placo-dep]"]
 intelrealsense = [
-    "pyrealsense2>=2.55.1.6486,<2.57.0 ; sys_platform != 'darwin'",
-    "pyrealsense2-macosx>=2.54,<2.57.0 ; sys_platform == 'darwin'",
+    # Upper bounds are relaxed in conda-forge; if needed they are handled via repodata patching.
+    "pyrealsense2>=2.55.1.6486",
+    # Platform selectors are not needed in conda-forge as pyrealsense2 is available on all platforms.
+    # "pyrealsense2-macosx>=2.54 ; sys_platform == 'darwin'",
 ]
 phone = ["hebi-py>=2.8.0,<2.12.0", "teleop>=0.1.0,<0.2.0", "fastapi<1.0", "lerobot[scipy-dep]"]
 
@@ -172,43 +180,24 @@ video_benchmark = ["scikit-image>=0.23.2,<0.26.0", "pandas>=2.2.2,<2.4.0"]
 
 # Simulation
 # NOTE: Explicitly listing scipy helps flatten the dependecy tree.
-aloha = ["gym-aloha>=0.1.2,<0.2.0", "lerobot[scipy-dep]"]
+aloha = ["gym-aloha>=0.1.2,<0.2.0"]
 pusht = ["gym-pusht>=0.1.5,<0.2.0", "pymunk>=6.6.0,<7.0.0"] # TODO: Fix pymunk version in gym-pusht instead
 libero = ["lerobot[transformers-dep]", "hf-libero>=0.1.3,<0.2.0; sys_platform == 'linux'", "lerobot[scipy-dep]"]
 metaworld = ["metaworld==3.0.0", "lerobot[scipy-dep]"]
 
 # All
 all = [
-    # NOTE(resolver hint): scipy is pulled in transitively via lerobot[scipy-dep] through
-    # multiple extras (aloha, metaworld, pi, wallx, phone). Listing it explicitly
-    # helps pip's resolver converge by constraining scipy early, before it encounters
-    # the loose scipy requirements from transitive deps like dm-control and metaworld.
-    "scipy>=1.14.0,<2.0.0",
     "lerobot[dynamixel]",
     "lerobot[gamepad]",
-    "lerobot[hopejr]",
-    "lerobot[lekiwi]",
-    "lerobot[reachy2]",
     "lerobot[kinematics]",
     "lerobot[intelrealsense]",
-    "lerobot[wallx]",
     "lerobot[pi]",
     "lerobot[smolvla]",
-    # "lerobot[groot]", TODO(Steven): Gr00t requires specific installation instructions for flash-attn
     "lerobot[xvla]",
-    "lerobot[hilserl]",
     "lerobot[async]",
-    "lerobot[dev]",
-    "lerobot[test]",
-    "lerobot[video_benchmark]",
     "lerobot[aloha]",
     "lerobot[pusht]",
-    "lerobot[phone]",
-    "lerobot[libero]; sys_platform == 'linux'",
-    "lerobot[metaworld]",
-    "lerobot[sarm]",
     "lerobot[peft]",
-    # "lerobot[unitree_g1]", TODO: Unitree requires specific installation instructions for unitree_sdk2
 ]
 
 [project.scripts]
